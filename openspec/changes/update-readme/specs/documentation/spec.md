## ADDED Requirements
### Requirement: Deployment Documentation
The documentation SHALL provide clear instructions for deploying the template and creating workspaces.

#### Scenario: User Guide
- **WHEN** a user reads the README
- **THEN** they MUST see the exact `coder templates push` command
- **AND** they MUST see the `coder create` command example
